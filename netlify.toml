[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback is not needed — this is a single-page static game.
# All assets are served directly from the dist directory.

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
